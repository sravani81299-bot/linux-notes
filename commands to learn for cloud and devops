1. Basic Linux Commands
📁 Navigation
pwd
ls
ls -l
ls -la
cd
cd ..
cd /
cd ~
📂 File & Directory Management
touch file.txt
mkdir folder
mkdir -p folder/subfolder
cp file1 file2
cp -r dir1 dir2
mv file1 file2
rm file
rm -r folder
rm -rf folder
📄 View File Content
cat file
tac file
head file
head -n 20 file
tail file
tail -f file   # live logs
less file
more file
🟦 2. File Permissions & Ownership
chmod 755 file
chmod u+x script.sh
chown user:user file
chown -R user:user folder
ls -l   # view permissions
umask
🟦 3. User & Group Management
useradd devuser
passwd devuser
su - devuser
groupadd devops
usermod -aG devops devuser
id devuser
whoami
who
🟦 4. Disk, Storage & File System Commands
df -h    # disk usage
du -sh folder
mount
umount
lsblk
fdisk -l
Archive & Compression
tar -cvf backup.tar folder
tar -xvf backup.tar
gzip file
gunzip file.gz
zip file.zip file
unzip file.zip
🟦 5. Networking Commands (Important for AWS EC2 Troubleshooting)
ifconfig
ip addr
ip route
ping google.com
curl google.com
wget url
nslookup domain.com
dig domain.com
host domain.com
Check open ports
netstat -tulnp
ss -tulnp
lsof -i :80
🟦 6. Process & Resource Monitoring
top
htop
free -h
ps -ef
kill -9 PID
killall processname
uptime
vmstat
iostat
🟦 7. Service Management (Systemd)
systemctl start nginx
systemctl stop nginx
systemctl restart nginx
systemctl enable nginx
systemctl disable nginx
systemctl status nginx
journalctl -u nginx
journalctl -xe
🟦 8. Package Management
Ubuntu / Debian
apt update
apt upgrade
apt install nginx
apt remove nginx
apt search package
Amazon Linux / CentOS / RHEL
yum update -y
yum install httpd
yum remove httpd
yum list installed
🟦 9. Logs (AWS EC2 Troubleshooting)
cd /var/log
cat /var/log/messages
cat /var/log/secure
journalctl -xe
dmesg
tail -f /var/log/nginx/error.log
tail -f /var/log/httpd/access.log
🟦 10. Shell Scripting Commands
#!/bin/bash
echo "Hello"
VAR=value
echo $VAR

if [ condition ]; then
   commands
fi

for i in {1..5}; do
   echo $i
done

function hello() {
   echo "Hello"
}
hello
Cron Jobs
crontab -e
crontab -l
🟦 11. SSH & Keys (Critical for AWS EC2)
ssh -i key.pem ec2-user@ip
ssh-copy-id user@server
scp -i key.pem file user@server:/path
chmod 400 key.pem
🟦 12. Git Commands (DevOps Essential)
git init
git clone URL
git status
git add .
git commit -m "message"
git push origin main
git pull
git branch
git checkout -b feature
git merge branch
🟦 13. Docker Commands
docker --version
docker images
docker ps
docker ps -a
docker pull image
docker run image
docker run -d -p 80:80 nginx
docker exec -it container bash
docker logs container
docker stop container
docker rm container
docker rmi image
🟦 14. Kubernetes (kubectl) Commands
kubectl get pods
kubectl get svc
kubectl get nodes
kubectl describe pod podname
kubectl logs podname
kubectl apply -f deployment.yaml
kubectl delete -f deployment.yaml
🟦 15. Terraform Commands
terraform init
terraform plan
terraform apply
terraform destroy
terraform fmt
🟦 16. Ansible Commands
ansible all -m ping -i inventory
ansible-playbook site.yml -i inventory
ansible-vault encrypt file
ansible-vault decrypt file
🟦 17. Jenkins (Linux CLI)
systemctl start jenkins
systemctl stop jenkins
journalctl -u jenkins
cat /var/lib/jenkins/secrets/initialAdminPassword
